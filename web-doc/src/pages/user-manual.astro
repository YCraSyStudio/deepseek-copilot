---
import DocsLayout from "../layouts/DocsLayout.astro";
import { pageContent, sectionAnchor } from "../i18n";

const lang = "en";
const content = pageContent[lang]["user-manual"];
---
<DocsLayout title={content.title} description={content.description} lang={lang} currentSlug="user-manual">
  <h1>{content.title}</h1>
  <p class="lead">{content.lead}</p>
  {content.sections.map((section) => (
    <section>
      <h2 id={sectionAnchor(section.title)}>{section.title}</h2>
      <ul>{section.items.map((item) => <li>{item}</li>)}</ul>
    </section>
  ))}
</DocsLayout>
